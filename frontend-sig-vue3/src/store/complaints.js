import { defineStore } from 'pinia';import api from '@/services/api';export const useComplaintsStore=defineStore('complaints',{state:()=>({list:[]}),actions:{async listAll(){this.list=(await api.get('/complaints')).data},async create(d){const f=new FormData();Object.entries(d).forEach(([k,v])=>Array.isArray(v)?v.forEach(file=>f.append('attachments',file)):f.append(k,v));await api.post('/complaints',f)}}});